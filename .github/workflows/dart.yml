name: Multi-Platform Build

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

env:
  FLUTTER_VERSION: "3.24.0"
  VERSION_TAG: "v3.2.0"

jobs:
  # iOS 构建
  build-ios:
    name: iOS Build
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          architecture: x64

      - name: Install Dependencies
        run: flutter pub get

      - name: Update CocoaPods
        run: pod repo update
        working-directory: ios

      - name: Build iOS Release
        run: flutter build ios --release --no-codesign

      - name: Create IPA
        run: |
          mkdir Payload
          mv Runner.app/ Payload
          zip -qq -r -9 Agrisale-ios-${{ env.VERSION_TAG }}.ipa Payload
        working-directory: build/ios/iphoneos

      - name: Upload iOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: iOS-Build
          path: build/ios/iphoneos/Agrisale-ios-${{ env.VERSION_TAG }}.ipa

  # Android 构建
  build-android:
    name: Android Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'

      - name: Install Dependencies
        run: flutter pub get

      - name: Clean Build
        run: flutter clean

      - name: Get Dependencies Again
        run: flutter pub get

      - name: Fix share_plus build.gradle
        run: |
          python3 << 'PYEOF'
          import os
          import re
          
          # 修复 share_plus 插件的 build.gradle 文件
          share_plus_build_gradle = os.path.join(os.path.expanduser('~'), '.pub-cache', 'hosted', 'pub.dev', 'share_plus-12.0.1', 'android', 'build.gradle')
          if os.path.isfile(share_plus_build_gradle):
              print(f'找到 share_plus build.gradle: {share_plus_build_gradle}')
              with open(share_plus_build_gradle, 'r', encoding='utf-8') as f:
                  content = f.read()
              
              # 修复 compileSdk = flutter.compileSdkVersion 为 compileSdk = 35
              if 'compileSdk = flutter.compileSdkVersion' in content:
                  new_content = content.replace('compileSdk = flutter.compileSdkVersion', 'compileSdk = 35')
                  with open(share_plus_build_gradle, 'w', encoding='utf-8') as f:
                      f.write(new_content)
                  print('✓ 已修复 share_plus compileSdk 配置')
              elif 'compileSdk flutter.compileSdkVersion' in content:
                  new_content = content.replace('compileSdk flutter.compileSdkVersion', 'compileSdk 35')
                  with open(share_plus_build_gradle, 'w', encoding='utf-8') as f:
                      f.write(new_content)
                  print('✓ 已修复 share_plus compileSdk 配置')
              elif 'compileSdkVersion flutter.compileSdkVersion' in content:
                  new_content = content.replace('compileSdkVersion flutter.compileSdkVersion', 'compileSdkVersion 35')
                  with open(share_plus_build_gradle, 'w', encoding='utf-8') as f:
                      f.write(new_content)
                  print('✓ 已修复 share_plus compileSdkVersion 配置')
              else:
                  print('✓ share_plus build.gradle 中未发现需要修复的 compileSdk 配置')
          else:
              print(f'警告: 未找到 share_plus build.gradle: {share_plus_build_gradle}')
          PYEOF

      - name: Fix install_plugin namespace and AndroidManifest
        run: |
          python3 << 'PYEOF'
          import re
          import os
          
          home = os.environ.get('HOME', os.path.expanduser('~'))
          
          # 修复 build.gradle - 添加 namespace 和统一 JVM 目标版本
          build_gradle_path = os.path.join(home, '.pub-cache', 'hosted', 'pub.dev', 'install_plugin-2.1.0', 'android', 'build.gradle')
          if os.path.isfile(build_gradle_path):
              print(f'找到 build.gradle: {build_gradle_path}')
              with open(build_gradle_path, 'r', encoding='utf-8') as f:
                  content = f.read()
              
              has_changes = False
              
              # 添加 namespace
              if 'namespace' not in content:
                  pattern = r'(android\s*\{)'
                  replacement = r'\1\n    namespace "com.crazecoder.openfile"'
                  content = re.sub(pattern, replacement, content, count=1)
                  has_changes = True
                  print('✓ 已添加 namespace 到 build.gradle')
              else:
                  print('✓ build.gradle 已包含 namespace')
              
              # 修复 compileSdkVersion - 确保至少为 30（Java 9+ 要求），最好设置为 35
              if 'compileSdkVersion' in content:
                  # 检查当前的 compileSdkVersion 值
                  match = re.search(r'compileSdkVersion\s+(\d+)', content)
                  if match:
                      current_sdk = int(match.group(1))
                      if current_sdk < 35:
                          content = re.sub(r'compileSdkVersion\s+\d+', 'compileSdkVersion 35', content)
                          has_changes = True
                          print(f'✓ 已更新 compileSdkVersion 从 {current_sdk} 到 35')
                      else:
                          print(f'✓ compileSdkVersion 已设置为 {current_sdk}')
                  else:
                      # 如果格式不同，尝试替换
                      content = re.sub(r'compileSdkVersion\s*[^\n]+', 'compileSdkVersion 35', content)
                      has_changes = True
                      print('✓ 已设置 compileSdkVersion 为 35')
              else:
                  # 如果没有 compileSdkVersion，添加它（在 namespace 之后）
                  if 'namespace' in content:
                      pattern = r'(namespace\s+"[^"]+"\s*\n)'
                      replacement = r'\1    compileSdkVersion 35\n'
                      content = re.sub(pattern, replacement, content, count=1)
                      has_changes = True
                      print('✓ 已添加 compileSdkVersion 35')
                  elif 'android {' in content:
                      pattern = r'(android\s*\{)'
                      replacement = r'\1\n    compileSdkVersion 35'
                      content = re.sub(pattern, replacement, content, count=1)
                      has_changes = True
                      print('✓ 已添加 compileSdkVersion 35')
              
              # 统一 JVM 目标版本为 11（与项目配置一致）
              # 修复 compileOptions - 将 Java 1.8 改为 11
              if 'VERSION_1_8' in content:
                  content = re.sub(r'VERSION_1_8', 'VERSION_11', content)
                  has_changes = True
                  print('✓ 已更新 Java 版本从 1.8 到 11')
              
              # 修复 kotlinOptions - 将 jvmTarget 1.8 改为 11
              if 'jvmTarget' in content:
                  # 匹配 jvmTarget = "1.8" 或 jvmTarget = '1.8'
                  content = re.sub(r'jvmTarget\s*=\s*["\']1\.8["\']', 'jvmTarget = "11"', content)
                  if 'jvmTarget = "11"' in content or "jvmTarget = '11'" in content:
                      print('✓ kotlinOptions jvmTarget 已设置为 11')
                  else:
                      has_changes = True
                      print('✓ 已更新 kotlinOptions jvmTarget 到 11')
              
              # 如果没有 compileOptions，添加它
              if 'compileOptions' not in content and 'android {' in content:
                  # 在 android { 后添加 compileOptions
                  pattern = r'(android\s*\{)'
                  replacement = r'\1\n    compileOptions {\n        sourceCompatibility JavaVersion.VERSION_11\n        targetCompatibility JavaVersion.VERSION_11\n    }'
                  content = re.sub(pattern, replacement, content, count=1)
                  has_changes = True
                  print('✓ 已添加 compileOptions 配置')
              
              # 如果没有 kotlinOptions，添加它
              if 'kotlinOptions' not in content and 'compileOptions' in content:
                  # 在 compileOptions 后添加 kotlinOptions
                  pattern = r'(compileOptions\s*\{[^\}]*?\})'
                  replacement = r'\1\n    kotlinOptions {\n        jvmTarget = "11"\n    }'
                  content = re.sub(pattern, replacement, content, count=1)
                  has_changes = True
                  print('✓ 已添加 kotlinOptions 配置')
              
              if has_changes:
                  with open(build_gradle_path, 'w', encoding='utf-8') as f:
                      f.write(content)
                  print('✓ build.gradle 修复完成')
          else:
              print(f'警告: 未找到 build.gradle: {build_gradle_path}')
          
          # 修复 AndroidManifest.xml - 移除 package 属性
          manifest_path = os.path.join(home, '.pub-cache', 'hosted', 'pub.dev', 'install_plugin-2.1.0', 'android', 'src', 'main', 'AndroidManifest.xml')
          if os.path.isfile(manifest_path):
              print(f'找到 AndroidManifest.xml: {manifest_path}')
              with open(manifest_path, 'r', encoding='utf-8') as f:
                  content = f.read()
              
              # 移除 package 属性
              if 'package=' in content:
                  # 匹配 package="..." 或 package='...'
                  pattern = r'\s*package\s*=\s*["\'][^"\']*["\']'
                  new_content = re.sub(pattern, '', content)
                  
                  with open(manifest_path, 'w', encoding='utf-8') as f:
                      f.write(new_content)
                  print('✓ 已移除 AndroidManifest.xml 中的 package 属性')
              else:
                  print('✓ AndroidManifest.xml 中已无 package 属性')
          else:
              print(f'警告: 未找到 AndroidManifest.xml: {manifest_path}')
          

      - name: Restore Android keystore
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > android/app/agrisale-release.jks
            echo "✓ Keystore restored to android/app/agrisale-release.jks"
          else
            echo "⚠️ Warning: ANDROID_KEYSTORE_BASE64 secret not set, will use debug signing"
          fi

      - name: Create key.properties
        run: |
          if [ -n "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" ]; then
            cat <<EOF > android/key.properties
          storePassword=${{ secrets.ANDROID_STORE_PASSWORD }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          storeFile=agrisale-release.jks
          EOF
            echo "✓ key.properties created in android/ directory"
          else
            echo "⚠️ Warning: key.properties not created, will use debug signing"
          fi

      - name: Build Android APK
        run: flutter build apk --release

      - name: Rename APK File
        run: |
          mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/Agrisale-android-${{ env.VERSION_TAG }}.apk

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: Android-APK
          path: build/app/outputs/flutter-apk/Agrisale-android-${{ env.VERSION_TAG }}.apk

  # macOS 构建
  build-macos:
    name: macOS Build
    runs-on: macos-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          architecture: x64

      - name: Enable macOS Desktop
        run: flutter config --enable-macos-desktop

      - name: Install Dependencies
        run: flutter pub get

      - name: Update CocoaPods
        run: pod repo update
        working-directory: macos

      - name: Build macOS Release
        run: flutter build macos --release

      - name: Create macOS ZIP Archive
        run: |
          cd build/macos/Build/Products/Release
          zip -qq -r -9 ../../../../../Agrisale-macos-${{ env.VERSION_TAG }}.zip agrisale.app

      - name: Create macOS DMG
        run: |
          # 创建临时目录用于 DMG 内容
          mkdir -p dmg_temp
          cp -R build/macos/Build/Products/Release/agrisale.app dmg_temp/
          
          # 创建 Applications 文件夹的符号链接
          ln -s /Applications dmg_temp/Applications
          
          # 创建 DMG 文件
          hdiutil create -volname "Agrisale" \
            -srcfolder dmg_temp \
            -ov -format UDZO \
            Agrisale-macos-${{ env.VERSION_TAG }}.dmg
          
          # 清理临时目录
          rm -rf dmg_temp

      - name: Upload macOS ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: macOS-ZIP
          path: Agrisale-macos-${{ env.VERSION_TAG }}.zip

      - name: Upload macOS DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: macOS-DMG
          path: Agrisale-macos-${{ env.VERSION_TAG }}.dmg

  # Windows 构建
  build-windows:
    name: Windows Build
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'

      - name: Enable Windows Desktop
        run: flutter config --enable-windows-desktop

      - name: Install Dependencies
        run: flutter pub get

      - name: Build Windows Release
        run: flutter build windows --release

      - name: Create Windows ZIP Archive
        run: |
          Compress-Archive -Path "build\windows\x64\runner\Release\*" -DestinationPath "Agrisale-windows-${{ env.VERSION_TAG }}.zip"

      - name: Install Inno Setup
        run: |
          choco install innosetup -y

      - name: Create Inno Setup Script
        run: |
          $version = "${{ env.VERSION_TAG }}".TrimStart('v')
          @"
          [Setup]
          AppName=Agrisale
          AppVersion=$version
          AppPublisher=Agrisale Team
          AppPublisherURL=https://github.com/yourusername/agrisale
          DefaultDirName={autopf}\Agrisale
          DefaultGroupName=Agrisale
          OutputDir=.
          OutputBaseFilename=Agrisale-windows-${{ env.VERSION_TAG }}-installer
          Compression=lzma2
          SolidCompression=yes
          ArchitecturesAllowed=x64
          ArchitecturesInstallIn64BitMode=x64
          PrivilegesRequired=admin
          SetupIconFile=windows\runner\resources\app_icon.ico
          UninstallDisplayIcon={app}\agrisale.exe
          
          [Languages]
          Name: "english"; MessagesFile: "compiler:Default.isl"
          
          [Tasks]
          Name: "desktopicon"; Description: "{cm:CreateDesktopIcon}"; GroupDescription: "{cm:AdditionalIcons}"; Flags: unchecked
          
          [Files]
          Source: "build\windows\x64\runner\Release\*"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs createallsubdirs
          
          [Icons]
          Name: "{group}\Agrisale"; Filename: "{app}\agrisale.exe"
          Name: "{group}\{cm:UninstallProgram,Agrisale}"; Filename: "{uninstallexe}"
          Name: "{autodesktop}\Agrisale"; Filename: "{app}\agrisale.exe"; Tasks: desktopicon
          
          [Run]
          Filename: "{app}\agrisale.exe"; Description: "{cm:LaunchProgram,Agrisale}"; Flags: nowait postinstall skipifsilent
          "@ | Out-File -FilePath "setup.iss" -Encoding UTF8

      - name: Build Windows Installer
        run: |
          & "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" setup.iss

      - name: Upload Windows ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Windows-ZIP
          path: Agrisale-windows-${{ env.VERSION_TAG }}.zip

      - name: Upload Windows Installer Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Windows-Installer
          path: Agrisale-windows-${{ env.VERSION_TAG }}-installer.exe

  # 创建 GitHub Release
  create-release:
    name: Create Release
    needs: [build-ios, build-android, build-macos, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' || startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Display structure of downloaded files
        run: ls -la artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.VERSION_TAG }}
          name: "Agrisale ${{ env.VERSION_TAG }}"
          body: |
            ## Agrisale ${{ env.VERSION_TAG }}
            
            ### 支持平台
            - iOS: iPhone 和 iPad
            - Android: 手机和平板
            - macOS: Mac 桌面应用
            - Windows: Windows 桌面应用
            
            ### 主要功能
            - 产品库存管理
            - 采购销售管理
            - 客户供应商管理
            - 财务报表统计
            - AI 数据助手
            - 数据备份恢复
            - 多用户系统
            
            ### v3.2.0 更新内容 🚀
            
            #### ✨ 新功能
            - **库存统计筛选增强**：库存统计界面新增日期范围筛选功能，支持按日期范围查看产品交易记录
            - **供应商筛选优化**：库存统计界面新增"未分配供应商"筛选选项，方便查看未关联供应商的产品
            
            #### 🛡️ 重大鲁棒性修复
            - **产品数据一致性修复**：修复产品名称修改时相关记录（采购/销售/退货）未同步更新的问题，确保数据一致性
            - **级联删除优化**：产品删除时自动删除所有关联记录（采购/销售/退货），并显示详细警告信息
            - **供应商同步功能**：产品供应商修改时，可选择是否同步更新历史采购记录的供应商信息
            - **删除关联处理**：修复客户/供应商/员工删除后，相关记录的 ID 未正确设置为 0 的问题
            - **对话框崩溃修复**：修复编辑记录时，如果关联的产品/客户/供应商/员工已被删除导致的崩溃问题
            - **搜索功能增强**：搜索功能现在可以正确搜索"未知客户"、"未知供应商"、"未指定"等条目
            
            #### 🎨 UI 改进
            - **警告提示优化**：删除基础数据（产品/客户/供应商/员工）时，显示详细的关联记录警告信息
            - **对话框提示改进**：编辑记录时，如果关联实体已删除，显示友好的警告提示而非崩溃
            - **操作反馈优化**：产品编辑时，根据实际修改情况显示更精确的操作结果提示
            
            ### 📥 下载说明
            
            #### iOS 用户
            - 下载 `Agrisale-ios-${{ env.VERSION_TAG }}.ipa` 文件
            
            #### Android 用户
            - 下载 `Agrisale-android-${{ env.VERSION_TAG }}.apk` 文件
            
            #### macOS 用户（推荐 DMG）
            - **推荐**: `Agrisale-macos-${{ env.VERSION_TAG }}.dmg` - 标准安装包，带安装向导
            - **备选**: `Agrisale-macos-${{ env.VERSION_TAG }}.zip` - 绿色版本，解压即用
            
            #### Windows 用户（推荐 EXE）
            - **推荐**: `Agrisale-windows-${{ env.VERSION_TAG }}-installer.exe` - 安装向导，自动配置
            - **备选**: `Agrisale-windows-${{ env.VERSION_TAG }}.zip` - 绿色版本，解压即用
            
            ### 📲 安装说明
            
            #### iOS
            - 需要使用第三方工具如 AltStore 或企业证书安装 IPA 文件
            
            #### Android
            - 开启"未知来源应用"后直接安装 APK 文件
            
            #### macOS
            - **DMG 文件**（推荐）：
              1. 双击 DMG 文件挂载磁盘镜像
              2. 将 agrisale.app 拖拽到 Applications 文件夹
              3. 首次打开可能需要在"系统偏好设置 → 安全性与隐私"中允许
            - **ZIP 文件**：
              1. 解压 ZIP 文件
              2. 将 agrisale.app 拖拽到应用程序文件夹
              3. 首次打开可能需要在系统设置中允许
            
            #### Windows
            - **EXE 安装程序**（推荐）：
              1. 双击运行安装程序
              2. 按照安装向导提示完成安装
              3. 自动创建开始菜单和桌面快捷方式（可选）
              4. 可通过"添加/删除程序"卸载
            - **ZIP 文件**：
              1. 解压到任意文件夹
              2. 运行 agrisale.exe
              3. 可手动创建快捷方式
            
            ---
            
            完整跨平台支持 | 原生用户体验 | 开源免费
          files: |
            artifacts/iOS-Build/Agrisale-ios-${{ env.VERSION_TAG }}.ipa
            artifacts/Android-APK/Agrisale-android-${{ env.VERSION_TAG }}.apk
            artifacts/macOS-ZIP/Agrisale-macos-${{ env.VERSION_TAG }}.zip
            artifacts/macOS-DMG/Agrisale-macos-${{ env.VERSION_TAG }}.dmg
            artifacts/Windows-ZIP/Agrisale-windows-${{ env.VERSION_TAG }}.zip
            artifacts/Windows-Installer/Agrisale-windows-${{ env.VERSION_TAG }}-installer.exe
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

